// Injected by Vite at build time from package.json — see vite.config.ts.
//
// The double underscores are Vite's convention for a `define` replacement, and
// the name has to match the key in vite.config.ts exactly, so no-underscore-dangle
// has nothing to offer here: renaming it would break the substitution.
// eslint-disable-next-line no-underscore-dangle
declare const __APP_VERSION__: string
